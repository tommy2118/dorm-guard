# Name of your application. Used to uniquely configure containers.
service: dorm_guard

# Name of the container image. With a provider-native registry the
# `image` is just the short name; the registry.server value prefixes it.
image: dorm_guard

# Deploy to these servers.
#
# Public IPv4 of the `dorm-guard` droplet in nyc3 (s-1vcpu-1gb,
# ubuntu-24-04-x64). Substituted from the REPLACE-DROPLET-IP
# placeholder in Slice 6. If the droplet is destroyed and recreated
# this IP will change — rotate here and in Route 53 together.
servers:
  web:
    - 104.236.125.236
  # job:
  #   hosts:
  #     - 104.236.125.236
  #   cmd: bin/jobs

# Kamal's Thruster proxy terminates TLS via Let's Encrypt for the
# single public hostname. Matches `config.hosts` in production.rb —
# both sides must agree or the cert issuance or host allowlist fails.
# (The shared source of truth per-environment is the DORM_GUARD_HOST
# ENV var that production.rb reads; this YAML value is the Kamal-time
# host, which is static per deploy and does not read from ENV.)
proxy:
  ssl: true
  host: dorm-guard.com

# Where you keep your container images.
#
# Reusing the existing `nightloom` DOCR namespace — DigitalOcean
# enforces one registry per account, so a dorm-guard-scoped namespace
# is not available. Image path is registry.digitalocean.com/nightloom/
# dorm_guard. Namespace chosen in Slice 5 ADR (docs/decisions/
# pr-0021-kamal-deploy.md). DOCR uses the DigitalOcean API access
# token as both username and password — both fields reference the
# same KAMAL_REGISTRY_PASSWORD secret.
registry:
  server: registry.digitalocean.com/nightloom
  username:
    - KAMAL_REGISTRY_PASSWORD
  password:
    - KAMAL_REGISTRY_PASSWORD

# Inject ENV variables into containers (secrets come from .kamal/secrets,
# which sources .env at deploy time).
env:
  secret:
    - RAILS_MASTER_KEY
    - KAMAL_REGISTRY_PASSWORD
    - SMTP_USER_NAME
    - SMTP_PASSWORD
    - DORM_GUARD_ALERT_TO
  clear:
    # Public hostname — single source of truth, consumed by config.hosts,
    # config.host_authorization, and config.action_mailer.default_url_options.
    DORM_GUARD_HOST: dorm-guard.com

    # Outbound mailer sender. MUST be a verified SES identity while SES
    # is in sandbox mode — and the SES-SendOnly IAM policy attached to
    # dorm-guard-ses-smtp restricts `ses:FromAddress` to exactly this
    # address. Any other sender value will be rejected by SES at send
    # time. Verifying the dorm-guard.com domain as a full sending
    # identity (so we can use dorm-guard@dorm-guard.com here) requires
    # SPF/DKIM/DMARC DNS records — out of scope for Epic 3, tracked as
    # a follow-up issue.
    DORM_GUARD_MAIL_FROM: tommy.caruso2118@gmail.com

    # SMTP endpoint. Provider-neutral naming — currently pointed at
    # Amazon SES us-east-1. Swap providers via .env / kamal env push,
    # not via config changes, so the image stays portable.
    SMTP_ADDRESS: email-smtp.us-east-1.amazonaws.com
    SMTP_PORT: "587"

    # Run the Solid Queue Supervisor inside the web server's Puma process
    # to do jobs. When you start using multiple servers, split job
    # processing to a dedicated machine (separate Kamal accessory) —
    # doing that is Epic 7 territory.
    SOLID_QUEUE_IN_PUMA: true

    # Pinned to 1 to prevent Solid Queue recurring-scheduler double-fire.
    # Running `plugin :solid_queue` inside Puma under WEB_CONCURRENCY > 1
    # would start the scheduler N times and fire schedule_due_checks N×
    # per minute. The value is enforced here (not just documented in the
    # ADR) because "documented but not enforced" is a fragile control.
    WEB_CONCURRENCY: 1

# Aliases are triggered with "bin/kamal <alias>". You can overwrite arguments on invocation:
# "bin/kamal logs -r job" will tail logs from the first server in the job section.
aliases:
  console: app exec --interactive --reuse "bin/rails console"
  shell: app exec --interactive --reuse "bash"
  logs: app logs -f
  dbc: app exec --interactive --reuse "bin/rails dbconsole --include-password"

# Use a persistent storage volume for sqlite database files and local
# Active Storage files. The volume is named so day-one backups (a future
# ops epic) can bolt on without code changes.
volumes:
  - "dorm_guard_storage:/rails/storage"

# Bridge fingerprinted assets, like JS and CSS, between versions to avoid
# hitting 404 on in-flight requests. Combines all files from new and old
# version inside the asset_path.
asset_path: /rails/public/assets

# Configure the image builder.
builder:
  arch: amd64

  # # Build image via remote server (useful for faster amd64 builds on arm64 computers)
  # remote: ssh://docker@docker-builder-server
  #
  # # Pass arguments and secrets to the Docker build process
  # args:
  #   RUBY_VERSION: ruby-4.0.0

# Use a different ssh user than root
# ssh:
#   user: app
