<%= @site.name %> is degraded.

dorm-guard's most recent check of <%= @site.url %> returned a
response that was technically successful but slower than the configured
threshold, or otherwise flagged as unhealthy (for example, a TLS
certificate close to expiry).

Visit your dorm-guard dashboard for the latest check details and history.

--
This is an automated alert from dorm-guard.
